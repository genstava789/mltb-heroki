name: Check Code

on:
  push:

  pull_request:

  workflow_dispatch:

jobs:
  ruff:
    runs-on: ubuntu-24.04
    
    steps:
      - name: Checkout 
        uses: actions/checkout@main
      
      - name: Ruff
        uses: chartboost/ruff-action@v1
